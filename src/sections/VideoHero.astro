---
import video2 from "../assets/video/videoTest1_b.mp4";
import iqeaLogo from "../assets/iqea_logo.png";
import { Image } from "astro:assets";
---

<section class="Hero">
  <div class="videoHero">
    <div class="videoContainer">
      <video
        class="leVideo"
        src={video2}
        autoplay
        loop
        muted
        style="clip-path: polygon(100% 0, 100% 95%, 50% 100%, 0 95%, 0 0);"
      ></video>

      <div class="heroData" style="clip-path: polygon(100% 0, 100% 95%, 50% 100%, 0 95%, 0 0);">
        <div class="title">
          <Image src={iqeaLogo as any} alt="logo" class={"imgHero"} />
          <h1>Ingenieria</h1>
        </div>
          <p>
            Limpiamos el planeta, tenemos experiencia diseñando y construyendo
            sistemas de tratamiento de agua y procesos, resolviendo los retos
            más complejos. Contamos con un portafolio de soluciones concretas
            para tu proyecto.
          </p>
      </div>
    </div>
  </div>

  <style>
    .Hero{
    background-color: var(--PrimaryBgColor);

    }
    .videoHero {
      width: 100vw;
      position: relative;
      overflow: hidden; /* Añade esto para que el contenido no sobresalga */
    }

    .videoContainer {
      position: relative;
    }

    .leVideo {
      width: 100vw;
      height: calc( 100% - var(--nav-size));
      z-index: -1; /* Envía el video al fondo */
    }

    .heroData {
      background-color: rgba(0, 0, 0, 0.42); /* Ajusta la opacidad según lo que desees */
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 100%;
      height: 100%;
      text-align: center;
      color: rgb(255, 255, 255);
      box-shadow: 1px 1px 1px 1px rgba(0, 0, 0, 0.25);

      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .title {
      display: flex;
      margin: 0 auto;
      align-items: center;
      gap:20px;

      .imgHero {
        width: 180px;
        height: 50px;


      }
      h1 {
        font-size: 60px;
        /* width: 60%; */
        margin: 0;
      }
    }

    p {
      font-size: 26px;
      margin: 0;
      line-height: 40px;
      width: 60%;
      margin: 0 auto;
    }
  </style>
</section>
